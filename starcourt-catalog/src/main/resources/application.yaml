server:
  port: 0
spring:
  application:
    name: starcourt-catalog
  datasource:
    url: jdbc:mysql://localhost:3306/starcourt_catalog?characterEncoding=utf8
    username: root
    password: ''
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
